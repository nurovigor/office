import { css } from 'linaria';

export const officeBlock = css`
	padding: 0 20px;
	display: flex;
	justify-content: center;
	height: 100vh;
	position: relative;
`;

export const Btn = css`
	 {
		position: fixed;
		z-index: 100;
		& a {
			margin-right: 10px;
		}
	}
`;
